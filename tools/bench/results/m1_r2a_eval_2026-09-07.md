# M1 R2 / T6：评估与月报脚本（2026-09-07）

- 对应：`docs/实施计划.md` **4.2** 的「评估：查询集扩到 60 题；两阶段评估脚本；月报脚本（存储按字节分项）」、**2.4** 的检索与存储验收口径、**4.4** 的失败归因六类；出题格式与评分规则沿用 `docs/查询集草稿.md`
- 交付：**`tools/eval/`** 八个文件（`queryset.schema.md`、`queryset.example.json`、`make_synthetic_queryset.py`、`eval_stage1.py`、`eval_stage2.py`、`monthly_report.py`、`run_all.sh`、`README.md`）
- **本轮没有改 `core/` 与 `app/` 的任何 Swift 源码**，也没有改 `docs/` 下任何文件；只写 Python（标准库）与文档
- 本机：Apple M4 Air / 16 GiB / 无风扇 / macOS 26.6.2 / Xcode 26.6 / Swift 6.3.3（语言模式 v6）/ Python 3.14.7
- 单位口径：**MiB / GiB = 2²⁰ / 2³⁰**；内存一律取 `/usr/bin/time -l` 的 `peak memory footprint`
- **本文是第二轮定向修复后的版本**（§0 逐条对第一次验收列的 6 条非阻塞问题：修了 5 条，第 6 条记入未做项并说明原因）。全部数字来自**修复后重跑的同一次** `sh tools/eval/run_all.sh`：scratch `~/Library/Caches/brosis-build/m1-r2a-eval/`，2026-09-07 23:59:17 → 2026-09-08 00:01:50（**153 s**），原始输出 `results/` **58 项**（= 修复前的 45 项 + 新增 13 项：`stage1_example.*` 3 项、`stage2_real_{good,bad}.*` 8 项、`citation_basis_{probe,check}.json` 2 项），另有 1 项 `per_day_720_stderr.txt` 是 §2 那条边界命令单独抓的，合计 59 个文件
- **逻辑数字与修复前逐项相同**（把 `generated_at` / `db` / 耗时 / 新增字段剔掉后 `stage1.json` 与修复前的 `~/Library/Caches/brosis-build/m1-eval/results/stage1.json` **完全一致**）；变的只有随 `core/` 并发改动而变的字节数（见 §3 脚注）
- 另有三次**独立 scratch 的复跑**：① 同参数换 scratch 复跑（`~/Library/Caches/brosis-build/m1-r2a-verify/`，00:11:23 → 00:13:56 = **153 s**，`stage1` / `stage2` / `deletions` / `mutation_check` / `citation_basis_check` / `stats` / `check` / `stage1_example` / `stage2_real_*` **九份 JSON 逐字段完全相同**，只有路径串与耗时不同）；② `PER_DAY=2880` 冒烟（十一步全绿，**69 s**，`~/Library/Caches/brosis-build/m1-r2a-smoke/`）；③ `PER_DAY=720` 边界（第 2 步逐条列出 8 个问题后 **exit 1**）
- 本轮**没有启动 GUI、没有触发 TCC 或钥匙串弹窗、没有 sudo、没有改 xcode-select、没有 git commit**；`--provider anthropic` 写了但**没有启用**，本轮**零网络请求**（`stage2.json` 的 `egress_bytes = 0`）；项目目录 `find` 不到任何构建产物（`__pycache__` / `*.pyc` / `.build` / `DerivedData` / `.swiftpm` 全为空）

---

## 0. 第二轮定向修复

第一次验收列了 6 条非阻塞问题：**修了 5 条**（下表第 1–6 行——「文案」那一条含三处，拆成第 2、5 两行写），**第 6 条（下表第 7 行）记入未做项**并说明为什么不是改一行的事。

| # | 第一次验收指出的 | 怎么修的 | 证据 |
|---|---|---|---|
| 1 | `queryset.schema.md:118` 写「核对的是 `eval_stage1.py --check-corpus`」，但 `argparse` 里没有这个参数 | **把参数做出来**（不是删文案）：`--check-corpus` 在检索之前拿 `brosis-store stats` 的 `observations`（**含墓碑行**，删除前后都一样）与查询集 `corpus.observations` 比对，不等直接退出；活 / 墓碑数与 `deletions` 的预期条数并排列进报告，**只作参考不作判据**（删除未执行时墓碑本来是 0）。真实题没有 `corpus` 段自动跳过。`run_all.sh` 第 4 步与第 7b 步都带上了；schema.md §4 改成描述真实行为 | `results/stage1.json` 的 `corpus_check`：259,200 == 259,200、live 258,817、墓碑 383 == 删除计划 383、`observations_match = true`；`results/stage1_example.json` 的 `corpus_check.checked = false`（真实题跳过） |
| 2 | README「一键复现约 4 分钟」与实测不符 | 换成**实测表**：默认跑 **124 / 146 / 153 / 153 s**（四次：第一轮执行、第一轮验收、本次的两个独立 scratch；都含 `swift build -c release`），冒烟 `PER_DAY=2880` **69 s**；`run_all.sh` 头注同步 | 本次 23:59:17 → 00:01:50 = **153 s**、换 scratch 复跑 00:11:23 → 00:13:56 = **153 s**；冒烟 00:02:39 → 00:03:48 = **69 s** |
| 3 | `eval_stage2.py:223` `valid = [i for i in cited if i in rel] if rel else cited`：真实题 `relevant` 为空时**引什么都算有效**，`valid_citation_rate` 恒 1.0 | 新增 `citation_basis()` 三档退档：`relevant_ids`（有真值）→ `evidence_match`（真实题：引用要落在第一阶段判定「满足 `evidence` 期望证据」的 `evidence_matched_ids` 里）→ `packed_evidence`（前两档都没有时只拦编造的 id，**报告单独点名、不许拿去对 2.4 的 ≥ 0.95**）。`eval_stage1.py` 相应把 `evidence_matched_ids` 落进每题的结果 | `run_all.sh` 第 7b 步新增回归：好答案 **4/4 过、有效引用率 1.000**；坏答案（引「喂过但不满足期望证据」的 id 或不存在的 id）**0/4 过、0.000**（`results/citation_basis_check.json` 的 `ok = true`）。**对照变异**：把老逻辑抄回 scratch 再跑同一套坏答案 → **3/3 过、1.000**，正是被修掉的那个洞 |
| 4 | how_to_verify §5(b) 的删除对账变异配方（`del-03` 窗口 1h→2h）在**全新库**上不成立（生成侧和存储层一起变，两边都是 720，不报错） | README §2.1 换成正确配方：**只改期望值、不动删除动作**——把 `del-04` 的 `expect_observations_affected` 9 改成 10，对全新库跑 `apply-deletions` | 实测 **exit 1**，stderr「删除条数与生成侧模拟不符：del-04」，明细 `del-04` 期望 10 / 实际 9，其余三项相符（`~/Library/Caches/brosis-build/m1-r2a-mutdel/deletions_tampered.json`） |
| 5 | 文案三处：README「约 4 分钟」（同第 2 条）、`make_synthetic_queryset.py:235` 注释拿 `DAYS=7 sh run_all.sh` 举例（与「不要调 DAYS」冲突）、结果文件说 `PER_DAY=720` 时「`loc-01` / `loc-02` 真值为空」（实际 7 题） | 注释改成「直接拿 `gen` 子命令跑短 JSONL 时的防线，`run_all.sh` 不支持调小 DAYS」；`PER_DAY=720` 的描述改成实测的 **8 条问题**（`loc-01/02/03/05/06/11/20` 七题「期望可答但真值为空」+ `del-03`「删除前的真值不能为空」），退出码 **1** | `results/per_day_720_stderr.txt`（本次实测抓的 stderr 原文） |
| 6 | `eval_stage1.py:143` `evidence_match` 里 `tw["start_ms"] <= hit["ts"] < tw["end_ms"]`：真实题若把 `time_window` 只写一端（另一端 `null`）会 **TypeError 崩掉** | `null` 端当无界：拆成 `lo is not None and ts < lo` / `hi is not None and ts >= hi` 两个独立判断；`queryset.schema.md` §7 补了这句口径 | 把 `queryset.example.json` 的 `ex-det-01` 改成 `{"start_ms": null, "end_ms": 1788739200000}`：**修复后 exit 0**、该题 `evidence_matched = 10` / `precision@10 = 1.0` / 归入「通过」；把修复前那两行抄回 scratch 再跑同一份 → **exit 1，`TypeError: '<=' not supported between instances of 'NoneType' and 'int'`**（`~/Library/Caches/brosis-build/m1-r2a-eval/nullwindow/`） |
| 7 | （**本轮未修，记入未做项**）`eval_stage1.py:234` 「上下文裁剪」在有全量真值的分支只看摘要，不核对原文是否真的含子串 | 不在本轮定向修复清单里，也改不动一行就完：要让 `mut-04` 继续成立，得让 `mutate` 连库取原文挑一个「在原文尾部、不在摘要里」的子串（`mutate` 目前不碰库）。已写进 `tools/eval/README.md` §3 第 3 条与本文 §9 | — |

**没有做别的改动**：`core/` 与 `app/` 的 Swift 源码一行没动，`docs/` 一个字没改。

## 1. 做了什么

| # | 交付 | 一句话 |
|---|---|---|
| 1 | `queryset.schema.md` + `queryset.example.json` | 查询集格式 `brosis/queryset@1`：id、四类之一的 class、自然语言问题 `q`、实际发给检索的 `search`、`expect`（hit / none）+ 五种不可答成因、**期望证据**（正文子串 / 应用 / 时间窗 / url / path 任意组合）、真值 id、标准答案 + 自动判分用的 `answer_check`、来源标注、`holdout`。真实题（D12）按它填，`relevant` 留空即可 |
| 2 | `make_synthetic_queryset.py` | 从 `tools/proto/gen_synth_m1.py` 的 1 个月 JSONL 造 **60 题**（20 / 20 / 10 / 10），真值**对 JSONL 全量重算**；4 道「已删除」题用 `brosis-store delete` 真删出来并与生成侧模拟**对账**；另有 `mutate` / `verify-mutations` 两个子命令做失败归因的变异检验 |
| 3 | `eval_stage1.py` | 逐题 `search`（带时间窗 / 应用过滤）+ 整套 `search-batch`，算 **Recall@10 / Precision@10 / MRR@10** 按四类与留出 / 非留出分列；每题归入 4.4 的前三类 + 上下文裁剪 + 负例误报；可选 `--check-corpus` 核对「这个库就是这份语料建出来的」；出 JSON + Markdown |
| 4 | `eval_stage2.py` | 按第一阶段的证据 id 用 `brosis-store evidence` 取**原文**，拼成「问题 + 证据 + 评分规则 + 作答模板」的判题提示；provider `none`（默认，不联网）/ `anthropic`（`urllib`，密钥只读环境变量，调用前打印外发字节并要求 `--confirm-egress`）/ `file`（接别处产生的答案）；出 JSON + Markdown，未评分时明确标注 |
| 5 | `monthly_report.py` | 读 stats JSON（`brosis-store stats --detail` **或** app 菜单「导出存储统计…」写的 `stats-<日期>.json`，别名递归解析、缺字段不崩），出月报：七项分项字节（2²⁰ / 2³⁰ 双列并注明）、按库内时间跨度折算月增长并对照 **0.6 GiB/月目标**与 **1 GiB/月上限**、行数与去重率 |
| 6 | `run_all.sh` | 十一步（编号 0–10）一键复现：构建 → 建库 → 造题 → 确定性复核 → 删除对账 → 第一阶段（带 `--check-corpus`）→ 变异检验 → 第二阶段 → 判分器自检（两套已知答案 + **真实题路径的引用判据回归**）→ 月报 ×2 → 汇总 |
| 7 | `tools/eval/README.md` | 用法与实测耗时、四类题怎么出的、失败归因判据、引用判据退档表、口径与 8 条已知限制 |

## 2. 怎么跑

```sh
# 一键（默认 scratch ~/Library/Caches/brosis-build/m1-eval/）
sh tools/eval/run_all.sh

# 验收者换自己的 scratch（会从零构建 + 建库）
SCRATCH=~/Library/Caches/brosis-build/verify-eval sh tools/eval/run_all.sh

# 冒烟：只调 PER_DAY，不要调 DAYS
PER_DAY=2880 sh tools/eval/run_all.sh
```

**实测耗时**（M4 Air 16 GiB，都含 `swift build -c release`）：

| 跑法 | 实测 | 出处 |
|---|---:|---|
| 默认 30 天 × 8,640 = 259,200 条 | **124 s / 146 s / 153 s / 153 s** | 第一轮执行 / 第一轮验收 / 本次（23:59:17 → 00:01:50）/ 本次换独立 scratch 复跑（00:11:23 → 00:13:56） |
| `PER_DAY=2880`（86,400 条）冒烟 | **69 s** | 本次 00:02:39 → 00:03:48 |

大头是建库（生成 JSONL 38.52 s + 导入 50.88 s = 89 s），`SKIP_BUILD=1` 省掉的构建约 15 s。

> `DAYS` 不能调小：`gen_synth_m1.py` 的种植按「第 N 周」与「最近 7 天」分布，少于 28 天就有查询词一条都种不出来。`PER_DAY` 不要低于 2880：活动定位那 6 题是「某应用 + 3 小时窗口」，窗口里观察太少时那个应用可能一次都没出现。**实测 `PER_DAY=720`**：第 2 步逐条列出 8 个问题（`loc-01` / `loc-02` / `loc-03` / `loc-05` / `loc-06` / `loc-11` / `loc-20` 七题「期望可答但真值为空」，外加 `del-03`「删除前的真值不能为空」）后 **exit 1**，不会静默出坏题（`results/per_day_720_stderr.txt`）。

## 3. 语料与库（与 R1 同一份，可对照）

| 项 | 数字 | 出处 |
|---|---:|---|
| 观察数 | **259,200**（30 天 × 8,640） | `results/gen_synth.json` |
| JSONL | 658,825,878 字节 = **628.3 MiB**，SHA-256 `642ba490…fa121d02`（**与 R1 逐字节相同**） | 同上 / `results/jsonl_size.txt` |
| 生成耗时 / 峰值 footprint | 38.52 s / **39.47 MiB**（41,386,464 B） | `results/gen_synth.json` / `gen_synth.time` |
| 导入 | 50.88 s，**5,097 条/s**，峰值 footprint **818.2 MiB**（857,966,224 B） | `results/import.json` / `import.time` |
| 删除后库文件 | 533,037,056 = **508.34 MiB**（WAL 0、SHM 32 KiB、freelist 0） | `results/stats.json` |
| 一致性 | `check` **13 项悬空引用全 0**、`integrity_check` ok、`fts_integrity_check` ok | `results/check.json` |
| `brosis-store`（release） | 3,796,528 字节 = 3.6207 MiB | `results/binary_size.txt` |
| 语料核对（`--check-corpus`） | 库里 **259,200** 条观察（含墓碑）== 查询集 `corpus.observations`；活 258,817 / 墓碑 383 == 删除计划 383 | `results/stage1.json` 的 `corpus_check` |

> 字节数的脚注：`core/` 在第一轮验收之后被别的任务动过（多了 MCP 审计表与 migrations v2），同一份语料现在建出来的库比第一轮大 49,152 B（`db_file` 532,987,904 → 533,037,056、索引 +32,768、元数据 +16,384、页数 32,531 → 32,534），`brosis-store` 也从 3,317,424 B 长到 3,796,528 B。**月增长两者都是 0.4964 GiB/月，结论不变**；本文全部字节数取本次（`core/` 当前状态）的值。

## 4. 60 题查询集

| 类 | 题数 | 怎么出的 | 真值规模（min / max / 合计） |
|---|---:|---|---|
| 活动定位 | **20** | 6 题「某应用 + 3 小时窗口」、4 题「某站点 + 当天」、3 题「某文件 + 6 小时」、5 题周次标记（其中 1 题是「全月都出现的词 + 只查第 1 周」）、2 题窗口标题 | 14 / 405 / 3,714 |
| 原文细节 | **20** | 中文三字以上 5、英文 5、代码标识符 5、数字与错误码 4、**单个汉字 2**（走 1–2 字扫描通道） | 6 / 35 / 304 |
| 跨来源 | **10** | 证据跨 ≥ 2 个 bundle_id 的词，**生成时断言**，不满足直接报错 | 6 / 24 / 118 |
| 无答案或已删除 | **10** | 6 道负例（FTS / 扫描 / 精确字段三条通道各覆盖，生成时断言全库 0 命中）+ 4 道已删除 | 0 / 0 / 0 |
| **合计** | **60** | 可答 50 + 不可答 10；**留出题 18 道**（每类按 id 排序取第 3、6、9…，确定性） | 4,136 |

真值口径与 `gen_synth_m1.py` 一致：**对 JSONL 全量重算**，不读它自己算的 truth、不靠种植记录推断；再按题目的时间窗裁一次，最后扣掉下面删掉的观察。

**确定性**：同一份 JSONL 连生成两次，去掉 `generated_at` 后 SHA-256 相同（`1518d23b…361168d6`，`results/determinism.json`）——脚本里没有随机数。

### 4.1 四道「已删除」题是真删出来的，而且和存储层对账

| 题 | 删除入口 | 生成侧模拟条数 | CLI 实际 `observations_affected` | 一致 | 连带删除的文本版本 / FTS 行 |
|---|---|---:|---:|:--:|---:|
| `del-01` | `delete --observations`（含「蟠桃」的那几屏） | 9 | **9** | ✔ | 9 / 9 |
| `del-02` | `delete --observations`（含 zygomorphic） | 5 | **5** | ✔ | 5 / 5 |
| `del-03` | `delete --range`（第 21 天 03:00–04:00 整点一小时） | 360 | **360** | ✔ | 71 / 71 |
| `del-04` | `delete --object host=paper.internal.example` | 9 | **9** | ✔ | 9 / 9 |

合计删掉 **383** 条观察（库里留 383 个墓碑，`live_observations` 258,817）。这张表是「**生成侧模拟的删除语义 == 存储层真实语义**」的自检：不等就 `SystemExit` 退出，`run_all.sh` 直接失败。出处 `results/deletions.json`。

## 5. 第一阶段：Recall@10 / Precision@10 / MRR@10

| 类 | 可答题 | 不可答题 | Recall@10 | Precision@10 | MRR@10 | 负例误报 | 通过 |
|---|---:|---:|---:|---:|---:|---:|---:|
| 活动定位 | 20 | 0 | **1.000** | 1.000 | 1.000 | — | 20/20 |
| 原文细节 | 20 | 0 | **1.000** | 1.000 | 1.000 | — | 20/20 |
| 跨来源 | 10 | 0 | **1.000** | 1.000 | 1.000 | — | 10/10 |
| 无答案或已删除 | 0 | 10 | — | — | — | **0** | 10/10 |
| **合计** | **50** | **10** | **1.000** | **1.000** | **1.000** | **0** | **60/60** |
| — 留出题 | 15 | 3 | 1.000 | 1.000 | 1.000 | 0 | 18/18 |
| — 非留出题 | 35 | 7 | 1.000 | 1.000 | 1.000 | 0 | 42/42 |

- 目标（2.4）：可答题 **Recall@10 ≥ 0.90**，实测 **1.000**，达标。
- **10 道不可答题一条证据都没返回**（6 道负例 + 4 道已删除），`none_with_false_positives = 0`。
- 整套 `search-batch` 在一个进程里跑完 **0.582 s**；逐题 `search` 与 `search-batch` 的证据 id **逐题完全一致**（`batch_vs_single_mismatch` 为空数组）。
- `max_fts_candidates` **120**、`fts_candidates_truncated_queries` **空**、最大摘要 **70 token**（预算 100）。
- 通道分布：`fts` 33 题、`exact_app` 6、`exact_url` 4、`exact_path` 3、`exact_title` 2、`scan` 2、空（不可答题）10。
- 出处：`results/stage1.json` / `results/stage1.md`。

> 单题耗时最长的四条（`search-batch` 里的单次采样，**不是延迟压测**——压测口径与冷 / 热 p50 / p95 见 R1 的 `m1_r1_retrieval_ledger_2026-09-07.md` §7）：`鸩` 143.45 ms（单字扫描 7 天窗口且 0 命中，最坏情况：整窗扫完一条不中）、`砚` 82.87 ms、`host:paper.internal.example` 61.88 ms（对象已删，两步式第一步就空集）、`熵` 55.54 ms。全套 60 题的单题耗时合计 567 ms。

> `eval_stage1.py` 整个进程本次 **2.96 s**（`results/stage1.time`），比修复前的 1.86 s 多出来的正好是 `--check-corpus` 那一次 `brosis-store stats`（单独计时 **1.06 s**：它要过一遍 `dbstat`）。

## 6. 失败归因分类是真的能分对（变异检验）

60 题全过意味着「失败归因」那张表是空的，**空表证明不了分类器是对的**。所以 `run_all.sh` 第 5 步故意造四道注定失败的题，每个桶各踩一次，再逐题核对：

| 变异题 | 怎么造的 | 真值 / 返回 | Recall@10 | 期望桶 | 实际桶 |
|---|---|---|---:|---|---|
| `mut-01-not-captured` | 问一个语料里根本没有的词 | 0 / 0 | — | 未采集 | **未采集** ✔ |
| `mut-02-expired` | 真值填 `del-01` 删掉的那 9 条 | 9 / 0 | 0.000 | 已过期或已删除 | **已过期或已删除** ✔（探活：9 个 id 全进 `missing`、0 个活着） |
| `mut-03-index-miss` | 真值里掺一个「活着但这条查询召不回」的 id | 2 / 10 | 0.500 | 索引漏召回 | **索引漏召回** ✔（探活：漏掉的 1 个 id 活着） |
| `mut-04-context-trim` | 证据全召回，但期望子串不在任何一条 ≤ 100 token 摘要里 | 340 / 10 | 1.000 | 上下文裁剪 | **上下文裁剪** ✔ |

`results/mutation_check.json` 的 `all_match = true`；不一致时 `verify-mutations` 直接退出非零，`run_all.sh` 失败。

## 7. 第二阶段：证据打包好了，**未评分**

| 项 | 数字 |
|---|---:|
| 有证据的题 | **50 / 60**（10 道不可答题按设计一条证据都没有） |
| 证据条数 / 字符 | **250** 条（每题前 5 条、邻居 ±1）/ **368,613** 字符 |
| 判题提示总字节 | **688,275**（672.1 KiB），逐题落在 `results/stage2/prompts/<id>.md` |
| 证据被截断的题 | **无**（上限 6,000 字符/条，本轮 250 条证据平均 1,474 字符） |
| 外发字节 | **0**（`provider = none`） |

**本轮第二阶段没有评分**：M1 还没有把本地叙述模型接进评估，线上 provider 要显式授权。判分逻辑本身用两套「已知答案」离线验过（`--provider file`，第 7 步）：

| 自检 | 结果 |
|---|---|
| 好答案（抄标准答案 + 引真值 id） | **60 / 60 通过**，有效证据引用率 **1.000** |
| 坏答案（可答题硬说无证据、不可答题瞎编一个事实并乱引） | **0 / 60 通过** |

出处 `results/stage2.json`、`results/stage2_selftest_good.json`、`results/stage2_selftest_bad.json`。

### 7.1 真实题（`relevant` 留空）的引用判据回归 —— 本轮修的第 3 条

合成集有全量真值，所以「引用有没有效」一直是拿真值判的；**真实题 `relevant` 是空的**，修复前的代码在这条路径上把「引用了什么」直接当成「引用有效」，有效引用率恒 1.000（第一次验收指出的洞）。现在按 `citation_basis` 退档，`run_all.sh` 第 7b 步用 `queryset.example.json` 每次都回归一遍：

| 答案集 | 引的是什么 id | 判据档 | 通过 | 有效引用率 |
|---|---|---|---:|---:|
| 好答案 | 第一阶段判定「满足期望证据」的 id | `evidence_match` ×3 | **4 / 4** | **1.000** |
| 坏答案 | `ex-cross-01` 引「喂过但不满足期望证据」的 `169550`；另两题引根本不存在的 `999999999` | `evidence_match` ×3 | **0 / 4** | **0.000** |
| 坏答案 × **老逻辑**（把修复前那一行抄回 scratch） | 同上 | — | **3 / 3 可答题全过** | **1.000** |

前两行出自 `results/citation_basis_check.json`（`ok = true`，`run_all.sh` 不满足就退出非零）；第三行是对照变异，代码副本在 `~/Library/Caches/brosis-build/m1-r2a-eval/mut/eval_stage2_old.py`（**只在 scratch 里改，项目目录没动**）。

## 8. 月报

### 8.1 来源一：`brosis-store stats --detail`

| 分项 | 字节 | MiB (2²⁰) | GiB (2³⁰) |
|---|---:|---:|---:|
| 原文（`text_versions` b-tree） | 250,200,064 | 238.61 | 0.2330 |
| — 其中原文净载荷（配额口径） | 218,135,383 | 208.03 | 0.2032 |
| 索引合计（含全文索引） | 242,286,592 | 231.06 | 0.2257 |
| — 其中全文索引 `text_fts` | 130,875,392 | 124.81 | 0.1219 |
| 元数据 | 40,386,560 | 38.52 | 0.0376 |
| 空闲页 | 0 | 0.00 | 0.0000 |
| **库文件合计** | **533,037,056** | **508.34** | **0.4964** |
| WAL / SHM | 0 / 32,768 | 0.00 / 0.03 | ~0 |
| 临时空间 | 0 | 0.00 | 0.0000 |
| 缩略图 | 0（本合成库不产缩略图） | 0.00 | 0.0000 |
| 模型资产 | 未提供（不在库里，要 `--models-dir` / `--models-bytes`） | — | — |

- **月增长**：库内时间跨度 **30 天**（`ledger --days`：2026-08-08 … 2026-09-06，30 个有观察的自然日），永久占用 = 库文件 + 缩略图 = 508.34 MiB → **0.4964 GiB/月**，**对照 0.6 GiB/月目标（D21）达标、对照 1 GiB/月上限（2.4）在上限内**。
- **行数与去重**：观察 259,200（未删 258,817 + 墓碑 383）、文本版本 84,746、出现记录 221,629、FTS 行 84,746、应用 6、删除审计 4；**去重率 = 1 − 84,746 / 221,629 = 0.6176**；全文索引 / 净载荷 **0.6000×**；原文 b-tree / 净载荷（页开销）**1.1470×**。
- 出处 `results/monthly_report.md` / `.json`（另有占用最大的 12 个 b-tree 明细表）。

### 8.2 来源二：app 导出的 `stats-<日期>.json`

T7 这一轮加的菜单「导出存储统计…」（`app/README.md` 8.8）写的是 `store` 对象 + `dbstat` 数组、外面套 `schema_version` / `generated_by` / `device_id` / `exported_at(_ms)`——**字段名与 CLI 相同但嵌套不同、明细数组换了名字**。`run_all.sh` 第 9 步按那个形状造一份（字节数取自同一份真 stats），月报脚本照样解析：来源行正确识别成「app 导出「导出存储统计…」（schema_version=1）」，明细表从 `dbstat` 读出来，`missing_fields` 只剩 `days` / `first_ts` / `last_ts`。补上缩略图 12,582,912 B 与模型资产 3,710,852,301 B（`--thumbs-bytes` / `--models-bytes`）后，月增长 **0.5081 GiB/月**（其中库文件 0.4964、缩略图 0.0117；模型资产是一次性资产，按口径不计入月增长）。出处 `results/monthly_report_appexport.md` / `.json`。

### 8.3 真实查询集的路径也跑通了（本轮起并进 `run_all.sh` 第 7b 步）

`tools/eval/queryset.example.json`（`source = real`、`relevant` 留空）对这个库跑第一阶段：**4 题全过**，判定自动切到 `judged_by = evidence_match`，Recall 记 `null`、Precision@10 **0.933**、MRR **1.000**——`ex-cross-01` 只匹配 8/10 是因为示例里 `evidence.apps` 只写了两个 bundle_id，第三个应用的两条不算数。`corpus_check` 显示 `checked = false`（真实题没有 `corpus` 段，`--check-corpus` 自动跳过）。这正是真实题（D12）填完之后的运行方式。

第一轮这一条要手敲一条命令，本轮已经并进 `run_all.sh` 第 7 步（连着 §7.1 的引用判据回归一起跑），出处 `results/stage1_example.json` / `.md`。

## 9. 未做与原因

| # | 未做 | 原因 |
|---|---|---|
| 1 | **第二阶段没有真的评分** | 需要一个会答题的 provider。本地叙述模型（D19 的 Qwen3.5-4B）M1 还没接进评估管线；线上 provider 要你显式授权，本轮按硬约束**零网络请求**。判分逻辑已用两套已知答案离线验过（§7） |
| 2 | **60 题是合成的，不是真实召回率** | 词是种进语料的、真值是算出来的，只能证明检索方法与评估流程成立。计划 4.2 要的真实 60 题需要你按 `queryset.schema.md` 填真实事件（D12），脚本已经能直接吃 |
| 3 | 没有量「Agent 会不会提对问题」 | 第一阶段的检索串是题目里写死的 `search.q`，量的是「给定查询串，检索层能不能拿到证据」 |
| 4 | 没跑 3 / 12 个月规模、没跑延迟压测 | 规模压测与延迟是 4.2 里另一条（R1 §7 已在 1 个月库上做过分层目标对照），不在本任务范围 |
| 5 | 月报的「缩略图 / 模型资产」在合成库上是 0 / 未提供 | 合成流不产缩略图，模型权重不在库目录里；两者都留了命令行入口（`--thumbs-dir` / `--models-dir` 或直接给字节），§8.2 演示了补进来的效果 |
| 6 | `已过期` 与 `用户删除` 分不开 | 见下面的 CLI 缺口第 1 条。归因桶因此叫「已过期或已删除」而不是 4.4 的「已过期」 |
| 7 | **「上下文裁剪」这一桶在有全量真值的分支只看摘要**（`eval_stage1.py` 里「真值都召回了、但摘要与片段都没有答案子串」就归这一桶，不核对原文是不是真的含它） | 第一次验收指出的第 6 条，**本轮定向修复清单里没有它**，而且不是改一行的事：`mut-04` 现在用的子串本来就不在证据原文里，一旦加上「原文必须含子串」的核对，那道变异题就会从「上下文裁剪」掉进「未采集」，`verify-mutations` 会挂。要补严得让 `mutate` 连库取原文、挑一个「在原文尾部、不在 ≤ 100 token 摘要里」的子串（`mutate` 目前完全不碰库，只读三份 JSON）。真实题走的是 `probe_full_text` 分支，本来就会核对原文，不受影响。已同时写进 `tools/eval/README.md` §3 第 3 条 |

### 9.1 `brosis-store` / app 导出的缺口（评估侧遇到的，未改代码）

| # | 缺什么 | 影响 | 建议 |
|---|---|---|---|
| 1 | `get_evidence` 对「用户删除墓碑」与「配额过期物理删除」都只回 `missing`，CLI 也没有查 `deletions` 审计表的子命令 | 4.4 的「已过期」这一类分不出是配额过期还是用户自己删的，只能合并成一个桶 | 加个 `deletions --list`（或让 `evidence` 回 `missing_reason ∈ {tombstone, gone}`） |
| 2 | app 导出的 `stats-<日期>.json` 里**没有库内时间跨度**（第一条 / 最后一条观察的 ts） | 拿导出文件算月增长必须另外给 `--span-days` 或 `--ledger-days`，单靠导出文件跑不完 | 导出里补 `first_observation_ts` / `last_observation_ts` 两个整数字段（不含正文与路径，不破坏 8.8 的隐私断言） |
| 3 | `search` 没有 offset / 分页 | Recall 只能算到 `limit` 以内，想量 Recall@50 得把 `limit` 提到 50，没法分页取 | M2 若要 Recall@k 曲线再说，M1 不阻塞 |
| 4 | `search-batch` 的结果里没有 `snippet` / `summary` | 判「上下文裁剪」必须再逐题跑一遍 `search`（本轮 stage1 全程 2.96 s = `--check-corpus` 的一次 `stats` 1.06 s + `search-batch` 0.58 s + 60 次单题 `search` 的进程开销） | 给 `search-batch` 加个 `--with-hits` 开关即可，非阻塞 |
| 5 | `brosis-store stats` 要过一遍 `dbstat`，一次 **1.06 s** | `--check-corpus` 只想要一个观察行数，却付了整份 `dbstat` 的钱 | 给 `stats` 加个 `--rows-only`（或让 `--detail` 才算 `dbstat`），非阻塞 |

## 10. 对计划的影响

一句话：**4.2 的「两阶段评估脚本 + 60 题查询集 + 月报脚本」三件交付齐了，合成集上第一阶段 60 题 Recall@10 / Precision@10 / MRR 全 1.000、10 道不可答题零误报、月增长 0.4964 GiB/月在 0.6 GiB 目标内**，本轮定向修复之后**真实题那条路径（`relevant` 留空）的两个阶段判据都有了回归**；卡口只剩两处——真实 60 题要你按 `queryset.schema.md` 填（D12），第二阶段要接一个 provider 才能出「Agent 能不能答对」的数字。
